{
  "name": "frontline-roads-refactor",
  "version": "0.30.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "syntax": "find src tests -name '*.js' -print0 | xargs -0 -n1 node --check && node --check sw.js",
    "verify": "npm run syntax && npm test"
  }
}
